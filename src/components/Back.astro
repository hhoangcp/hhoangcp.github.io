---
interface Props {
  href?: string;
  label?: string;
}
const { href = '/', label = '← Quay lại' } = Astro.props;
---

<a
  class="post-back"
  href={href}
  onclick="if(document.referrer.startsWith(location.origin)){history.back();return false;}"
>
  {label}
</a>
